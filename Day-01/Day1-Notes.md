'''			   DAY 1 - PYTHON INTRODUCTION

Procedural language:
--> This follows a step-by-step approach, where code is structure into procedures such as function or routines...  
eg --> c

Object oriented procedural language:
--> This is based on concept object and class....
eg --> python, java
num = 89
num_2 = 67
print(id(num))
print(id(num_2))
total_ = num+num_2
print(id(total_))

what is python:
--> Dynamically typed - python knows the type of data we are passing to type variable...
--> Interpreter - python execute code in line-by-line, if any error happened in lines and it will be stops the exection in that and before will gives the output.. 
eg -
num = 89
num_2 = 67
print(id(num))
print(id(num_2))
total_ = num+num_2
print(id(total))
--> High level
1991 - guide van russum

why use pyhton:
--> easy syntax
--> cross-platform
--> wide application
--> huge library support

Tokens:
--> keywords - if, else, while (reserved words)
--> identifiers - names given to the variables,functions,class
--> literals - 89,"hello", 4.56 (right side part of varible declaration is called literals)
--> Operators - +,-,*,/
--> punctuators - {},[],()

Rules of variables:
A-Z, a-z, _ are good ways to use in python
Special Characters at first, number at first, space are bad ways to use in python and gives you errors
eg - $num =0, 2sum = 9, s 0 = 89 are the bad ways
   - num, num_ are good ways 

Comment:
--> single line comment -#
--> multi line comment -''' ''' '''
